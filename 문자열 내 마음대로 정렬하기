#include <string>
#include <vector>
#include <algorithm>

using namespace std;

vector<string> solution(vector<string> strings, int n) {
    // 문자열 벡터를 정렬
    sort(strings.begin(), strings.end(), [n](const string& a, const string& b) {
        // n번째 문자가 다르면 그 기준으로 정렬
        if (a[n] != b[n]) return a[n] < b[n];
        // 같으면 사전순 정렬
        return a < b;
    });

    return strings;
}
